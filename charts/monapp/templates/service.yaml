apiVersion: v1
kind: Service
metadata:
  name: {{ include "monapp.fullname" . }}
  labels:
    {{- include "monapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monapp.selectorLabels" . | nindent 4 }}
